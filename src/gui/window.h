#ifndef WINDOW_H
#define WINDOW_H

#include "control.h"
/*
class Window : public Control {
public:
	Window();
	~Window();

	void AddChild( Control *child );
	void Move( int xChange, int yChange );
	void Close();
protected:
private:
	struct tControlList{
		Control* control;
		tControlList* next;
	} *list;
};
*/
#endif
